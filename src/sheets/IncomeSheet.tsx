import { StyleSheet, Text, View } from "react-native";
import React, { memo } from "react";

const IncomeSheet = memo(() => {
    return (
      <View>
        <Text>IncomeSheet</Text>
      </View>
    );
  });

  IncomeSheet.displayName="IncomeSheet";

export { IncomeSheet };

const styles = StyleSheet.create({});